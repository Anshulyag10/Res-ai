# Frontend environment — Vite reads these at build time (VITE_ prefix required)
VITE_API_URL=http://localhost:8000
# Set this to match the API_KEY in the backend .env (leave empty if auth is disabled)
VITE_API_KEY=
